Name = "Settings",
	Icon = "rbxasset://textures/ui/Settings/MenuBarIcons/GameSettingsTab@2x.png",
	AutoRotate = false,
	IsAGame = false,
	ThemeColor = Color3.fromRGB(39, 37, 38),

	Run = function(self,parent)			
		local scrollingFrame = appHandler:Create(parent,{
			Type = "ScrollingFrame",
			Size = UDim2.new(1,0,0.9,0),
			Position = UDim2.new(0,0,0.1,0),
			BackgroundTransparency = 1,
			AutomaticCanvasSize = Enum.AutomaticSize.XY,
			ScrollBarThickness = 5,
			TopImage = "",
			BottomImage = ""
		})
		appHandler:Create(scrollingFrame,{
			Type = "UIListLayout",
			FillDirection = Enum.FillDirection.Vertical,
			HorizontalAlignment = Enum.HorizontalAlignment.Center,
			VerticalAlignment = Enum.VerticalAlignment.Top,
			SortOrder = Enum.SortOrder.LayoutOrder,
			Padding = UDim.new(0.05,0)
		})

		--APP TITLE
		appHandler:Create(scrollingFrame,{
			Type = "TextLabel",
			Text = "Settings",
			Size = UDim2.new(0.8,0,0.08,0),
			Position = UDim2.new(0.45,0,0.1,0),
			AnchorPoint = Vector2.new(0.5,0),
			Font = Enum.Font.SourceSansBold,
			TextScaled = true,
			TextColor3 = Color3.fromRGB(255,255,255),
			BackgroundTransparency = 1,
			TextXAlignment = Enum.TextXAlignment.Left,
		})

		--SYSTEM GROUPS

		local groupSize = UDim2.new(0.9,0,0.5,0)

		do -- DEVICEGROUP
			local deviceArea = appHandler:Create(scrollingFrame,{
				Type = "Frame",
				Name = "DeviceArea",
				Size = groupSize,
				Position = UDim2.new(0.1,0,0.2,0),
				BackgroundColor3 = Color3.fromRGB(107, 107, 107),
				BorderSizePixel = 0,
				LayoutOrder = 2,
				AutomaticSize = Enum.AutomaticSize.Y
			})
			appHandler:Create(deviceArea,{
				Type = "UICorner",
				CornerRadius = UDim.new(0.1,0),
			})
			appHandler:Create(deviceArea,{
				Type = "UIListLayout",
				FillDirection = Enum.FillDirection.Vertical,
				HorizontalAlignment = Enum.HorizontalAlignment.Center,
				VerticalAlignment = Enum.VerticalAlignment.Top,
				SortOrder = Enum.SortOrder.LayoutOrder,
				Padding = UDim.new(0.05,0)
			})

			-- ABOUT PHONE

			local aboutPhone = appHandler:Create(deviceArea,{
				Type = "Frame",
				Name = "AboutPhone",
				Size = UDim2.new(1,0,0.15,0),
				Position = UDim2.new(0,0,0.05,0),
				BackgroundColor3 = Color3.fromRGB(107, 107, 107),
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				LayoutOrder = 1,
				AutomaticSize = Enum.AutomaticSize.Y
			})
			appHandler:Create(aboutPhone,{
				Type = "ImageLabel",
				Name = "Icon",
				Image = `rbxassetid://5261794391`,
				Size = UDim2.new(0.13,0,0.5,0),
				Position = UDim2.new(0.1,0,0.55,0),
				AnchorPoint = Vector2.new(0.5,0.5),
				ResampleMode = Enum.ResamplerMode.Pixelated,
				BackgroundTransparency = 1,	
			})

			appHandler:Create(aboutPhone,{
				Type = "TextLabel",
				Text = "About Phone",
				Size = UDim2.new(0.9,0,0.8,0),
				Position = UDim2.new(0.65,0,0.1,0),
				AnchorPoint = Vector2.new(0.5,0),
				Font = Enum.Font.SourceSansBold,
				TextScaled = true,
				TextColor3 = Color3.fromRGB(255,255,255),
				BackgroundTransparency = 1,
				TextXAlignment = Enum.TextXAlignment.Left,
			})

			-------------
		end


		--utility:AddConnection(`{self.ProcessID} Main Loop`,runservice.RenderStepped:Connect(function()
		--end),self.ProcessID)
	end,
